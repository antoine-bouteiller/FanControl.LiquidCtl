[project]
name = "liquidctl-bridge"
version = "0.1.0"
description = ""
authors = [{ name = "Antoine", email = "anto.bouteiller@gmail.com" }]
requires-python = ">=3.11,<3.14"
dependencies = [
    "liquidctl (>=1.15.0,<2.0.0)",
    "pywin32 (>=310,<311)",
    "msgspec (>=0.19.0,<0.20.0)",
]

[tool.poetry.group.dev.dependencies]
ruff = "^0.14.0"
pytest = "^8.0.0"
pytest-mock = "^3.11.0"
pyinstaller = "^6.14.2"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
