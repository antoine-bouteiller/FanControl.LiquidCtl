[project]
name = "liquidctl-server"
dynamic = ["version"]
description = ""
authors = [{ name = "Antoine Bouteiller" }]
requires-python = ">=3.14,<3.15"
dependencies = [
  "liquidctl>=1.16.0,<2.0.0",
  "pywin32>=312,<313; sys_platform == 'win32'",
  "msgspec>=0.21.1,<0.22.0",
]

[dependency-groups]
dev = ["ruff>=0.16.0,<0.17.0", "nuitka>=4.2,<4.3", "pytest>=9.1,<9.2", "pytest-cov>=7.1,<7.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "../../package.json"
pattern = '"version":\s*"(?P<version>[^"]+)"'

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff.lint]
# USB/HID drivers raise anything; the service catches all to keep other devices alive.
ignore = ["BLE001"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["manual"]
